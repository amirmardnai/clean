# گزارش بررسی کامل فایل `clean.rar`

تاریخ بررسی: ۲۰۲۶-۰۹-۰۳
روش بررسی: استخراج کامل آرشیو RAR5، خواندن خط‌به‌خط همه‌ی ۹ فایل پایتون + ۳ اسکریپت شل + فایل بatches + فایل‌های JSON و فونت‌ها، تحلیل ایستای سینتکس، اسکن محرمانگی، و بازرسی چشمی PDF خروجی نمونه.

---

## ۱) آنچه درون بسته است (۲۹ ورودی، ۲۴ فایل واقعی)

| مسیر | حجم | نقش |
|---|---|---|
| `README.md` + `SKILL.md` | ۲۳KB | مستندات پایپ‌لاین مدولار (SKILL.md = تعریف یک Agent Skill) |
| `scripts/extract_pages.py` | ۴KB | مرحله ۱: استخراج صفحات از PDF/CBZ/CBR/پوشه |
| `scripts/ocr_pages.py` | ۶.۵KB | مرحله ۲: OCR با ابزار `apple-vision` و ساخت `worksheet.json` |
| `scripts/check_worksheet.py` | ۴.۵KB | مرحله ۲b: بازرسی کیفی جعبه‌ها و نویز OCR |
| `scripts/apply_translations.py` | ۳KB | مرحله ۳: پچ دسته‌ای ترجمه‌ها بر اساس id |
| `scripts/render_pages.py` | ۷.۵KB | مرحله ۴: پاک‌سازی حباب + تایپ‌ست فارسی RTL |
| `scripts/clean_lettering.py` | ۲۱KB | مرحله ۴b: پاک‌سازی ساختارمحور از روی artwork (بهترین کد بسته) |
| `scripts/typeset_lettering.py` | ۶KB | تایپ‌ست متن روی تصویر با stroke و شیب ایتالیک |
| `scripts/assemble_output.py` | ۳KB | مرحله ۵: خروجی PDF/CBZ با img2pdf |
| `scripts/*.sh` ×۳ | ~۹KB | setup، تست خودکار، oneshot تک‌صفحه‌ای |
| `translate_manhwa_pro.py` | ۲۸KB | **نسخه‌ی دوم و یکپارچه** (v3.0): EasyOCR + LLM + PDF/CBZ |
| `run_translate_all.bat` | ۱KB | رانر ویندوزی برای نسخه‌ی یکپارچه |
| `assets/fonts/Vazirmatn-*.ttf` | ۳۷۰KB | فونت وزیرمتن ۳ وزن (SIL OFL) — **سالم و قابل لود** |
| `assets/*.json` ×۲ | ۳KB | قالب glossary و نمونه worksheet |
| `[291] ... (Persian).pdf` | ۱۳.۶MB | **خروجی نمونه**: چپتر ۲۹۱ «Survival Story of a Sword King»، ۱۵ صفحه |
| `scripts/__pycache__/*.pyc` | ۴۵KB | فایل‌های کامپایل‌شده‌ی پایتون ۳.۱۳ — **اضافی، نباید در بسته باشد** |

**نکته‌ی ساختاری مهم:** بسته «دو پیاده‌سازی موازی» دارد که هم‌پوشانی‌اند و از هم ناآگاهند؛ مستندات فقط نسخه‌ی مدولار را توصیف می‌کند و نسخه‌ی یکپارچه (`translate_manhwa_pro.py`) هیچ مستندی ندارد:

| محور | نسخه‌ی مدولار (scripts/) | نسخه‌ی یکپارچه (translate_manhwa_pro.py) |
|---|---|---|
| OCR | `apple-vision` (فقط محیط اپل/مک) | EasyOCR با tiling برای استریپ‌های +۵۰هزار پیکسلی |
| ترجمه | دستی/مدل خارج از اسکریپت (worksheet) | خودکار با LLM از طریق relay محلی (Gemini) |
| پاک‌سازی حباب | پرکردن با رنگ حلقه‌ی بیرونی | Inpaint با OpenCV Telea + ماسک متن دقیق (بهتر) |
| شمای جعبه | `[x, y, w, h]` | `[x1, y1, x2, y2]` |
| فونت | Vazirmatn-Bold/ExtraBold همراه بسته | جست‌وجوی ویندوزی `Vazir*.ttf` — **فونت‌های bundle را نمی‌بیند!** |
| Shaping | `config_for_true_type_render` | `arabic_reshaper.reshape` ساده |
| اجرا | Alpine/iSH (setup.sh مبتنی بر apk) | ویندوز (run_translate_all.bat، لودر `py -3.13`) |

---

## ۲) نتایج تست‌هایی که انجام دادم

- ✅ استخراج: آرشیو RAR5 بود؛ با ماژول `unrar2-cffi` استخراج شد (unrar/unar/7z در محیط نبود).
- ✅ سینتکس: هر ۹ فایل `.py` با `py_compile` بدون خطا پاس شدند.
- ✅ فونت‌ها: هر سه فایل Vazirmatn سالم و توسط FreeType/Pillow لود شدند.
- ⚠️ فایل‌های `.pyc` برای CPython 3.13‌اند و روی نسخه‌های دیگر بی‌فایده/گمراه‌کننده.
- ✅ PDF نمونه: ۱۵ صفحه، ساخته‌ی `img2pdf 0.6.3` (یعنی خروجی **نسخه‌ی مدولار** است، نه نسخه‌ی Pro که با Pillow ذخیره می‌کند)، بدون لایه‌ی متن، صفحات 720×26652px.
- ✅ کیفیت خروجی نمونه (بازرسی چشمی صفحات ۱ و ۲): متن فارسی روان و محاوره‌ای، shaping/RTL درست، حباب‌ها تمیز پاک شده، ترجمه‌ها طبیعی («یعنی آتیسه از قبل جای دیگه‌ای قایمش نکرده بود؟»). بخش credit اسکنلیشن (demonicscans) دست‌نخورده باقی مانده — مطابق طراحی. چند SFX کره‌ای/انگلیسیِ پخته‌شده در تصویر ترجمه نشده‌اند (طبق مستندات عمدی است).

---

## ۳) مسائل بحرانی 🔴

### ۳.۱ کلید API در سورس (امنیتی — فوری)
```python
# translate_manhwa_pro.py:56
DEFAULT_LLM_KEY = "sk-2428d57044e946e18c4361e9ad45b0bf"
```
کلید در کد هاردکد شده و در خط ۷۰۷ هم به‌عنوان مقدار پیش‌فرض `--api-key` برمی‌گردد. هرکس آرشیو را دارد، کلید را دارد.
**اقدام:** کلید را فوراً revoke/rotate کنید؛ سپس پیش‌فرض را حذف کنید و فقط از `os.environ["MANHWA_API_KEY"]` بخوانید (در صورت نبود، پیام خطا).

### ۳.۲ لو رفتن مسیر شخصی
`run_translate_all.bat` خط ۱۱: مسیر `C:\Users\amir\AppData\...` نام کاربری ویندوز شما را در بسته‌ی توزیع‌شده افشا می‌کند.

### ۳.۳ حق نشر خروجی همراه بسته
PDF نمونه، ترجمه‌ی یک چپتر اسکنلیشن تجاری (با یادداشت demonicscans در خود فایل) است. خودِ README صریحاً گفته فقط روی متریالی استفاده کنید که حق ترجمه‌اش را دارید. قرار دادن این خروجی داخل بسته‌ی قابل توزیع ریسک حقوقی دارد — از بسته حذفش کنید.

---

## ۴) باگ‌ها و ریسک‌ها 🟠

### در `translate_manhwa_pro.py`
1. **کرش هنگام نبود EasyOCR:** با `try/except` مقدار `easyocr=None` می‌شود، ولی `main()` بدون چک صداش می‌زند (`easyocr.Reader(...)`) → `AttributeError` به‌جای پیام راهنما.
2. **`gpu=True` هاردکد** (خط ۷۵۱): روی ماشین بدون CUDA کرش/هشدار؛ `gpu = torch.cuda.is_available()` مناسب‌تر است. در آپشن‌ها هم فلگی برای CPU نیست.
3. **`resolve_font()` فونت‌های همراه بسته را نمی‌بیند:** فقط مسیرهای ویندوزی `Vazir*.ttf` و Noto را می‌گردد؛ نام فایل‌های bundle `Vazirmatn-*` است. نتیجه روی خیلی سیستم‌ها = فول‌بک به `"arial.ttf"` (بدون گلیف فارسی → توفو/مربع). نه‌ضرورتاً: فلگ `--font` هم در CLI وجود ندارد.
4. **حافظه‌ی ساخت PDF** (خط ~۶۵۱): همه‌ی صفحات هم‌زمان `Image.open` می‌شوند (`rest = [...]`)؛ برای چپترهای بلند ریسک OOM. img2pdf یا ذخیره‌ی تدریجی بهتر است.
5. **`max_tokens=4000` ثابت** مستقل از تعداد حباب‌های صفحه: در صفحات شلوغ، JSON خروجی برش می‌خورد → همه‌ی آن batch خالی و دست‌نخورده می‌ماند (رفتار امن، ولی بی‌صدا). بهتر است اندازه با تعداد آیتم مقیاس شود یا per-page retry با تقسیم batch.
6. **فیلتر `is_genuine_dialogue_line` دیالوگ واقعی را قربانی می‌کند:** هر رشته‌ی ≥۳ حرفِ بدون واکه (مثل `WTF`، `HMM`، `PSST`، `DM`) حذف می‌شود.
7. **حذف تکراریِ تایل‌ها ممکن است دیالوگ تکراری واقعی را حذف کند:** شرط «متن یکسان + فاصله‌ی عمودی < ۸۰px» دو حبابِ نزدیک با متن یکسان (مثل «نه!»/«نه!») را duplicate می‌شمارد.
8. **نام مدل `gemini-3.7-flash` و endpoint `http://127.0.0.1:8046/v1`:** اجرای صحیح کاملاً به راه‌اندازی دستی یک relay محلی وابسته است و هیچ جا مستند نشده؛ کاربر عادی با خطای اتصال مواجه می‌شود.
9. **جزئی:** CBZ با `ZIP_STORED` (بدون فشرده‌سازی)؛ فول‌بک pixmap با `dpi=150` ثابت؛ تصاویر <۲۰۰px نادیده گرفته می‌شوند؛ مرتب‌سازی `natural_key` روی نام `pg-0000-02`؛ مستندسازی صفر در README.

### در نسخه‌ی مدولار (scripts/)
1. **`CREDIT_HINTS` فالس‌پازیتیو جدی دارد** (`ocr_pages.py:27`): برچسب «credit» برای هر متن حاوی `team`، `scan`، `.com`... دیالوگ واقعی مثل *"team up"* یا *"scan the room"* به‌اشتباه credit می‌شود و **در رندر اسکیپ و ترجمه‌نشده باقی می‌ماند**.
2. **وابستگی سخت به `apple-vision`:** مستندات از «Linux/Al­pine/iSH» حرف می‌زند ولی مرحله‌ی ۲ بدون ابزار اختصاصی `apple-vision` (مک) غیرقابل اجراست؛ `setup.sh` روی هر سیستم غیر apk می‌شکند و `selftest_lettering.sh` هم برای وریفای OCR به همان ابزار نیاز دارد. آداپتور OCR جایگزین (Tesseract/EasyOCR) لازم است.
3. **`render_pages.py` پاک‌سازی با رنگ تخت** داخل حباب عالی ولی روی متنِ روی بافت «مستطیل خاکستری» می‌گذارد — در SKILL.md صادقانه مستند شده (ارجاع به ۴b) ولی `check_worksheet.py` هشدار خودکار نمی‌دهد.
4. **جزئی:** بکاپ `apply_translations.py` تک‌لایه است (`.bak` هر بار بازنویسی می‌شود)؛ رشد جعبه `--grow 1.15` می‌تواند با حباب همسایه تداخل کند (مستند)؛ مرتب‌سازی خطوط بر اساس `(y,x)` در چیدمان‌های دوسطحی می‌تواند ترتیب خواندن را به‌هم بزند.

---

## ۵) نکات مثبت (ارزش نگه‌داشتن) 🟢

- **`clean_lettering.py` مهندسی بسیار خوبی دارد:** ماسک گلیف با داوری دو قطبی (روشن/تیره)، طبقه‌بندی ناحیه flat/textured/fallback، کپی پیکسل واقعی در امتداد بافت به‌جای inpaint (با استدلال درست: «درون‌یابی همیشه صاف‌تر از اطرافش به‌نظر می‌رسد»)، تصحیح روشنائی درز با هرمی pull-push، و ماشین‌حساب کیفی عینی (`fill_quality`) به‌جای داوری چشمی.
- جداسازی تمیز «کار مکانیکی» از «کار قضاوت» در طراحی مدولار؛ همه‌ی مراحل JSON محور و قابل resume هستند.
- `check_worksheet.py` واقعاً مفید است (جعبه‌های کوچک/غول/خارج‌صفحه، هم‌پوشانی، نویز).
- رفتار «برخورد با failure با حفظ متن اصلی» در نسخه‌ی Pro درست طراحی شده (Zero-Blank-Bubble).
- شفافیت حقوقی در README/SKILL (برچسب `kind:"credit"` و هشدار درباره‌ی حذف attribution تیم‌های دیگر).
- تست خودکار (selftest) با دو سناریوی مصنوعی واقع‌بینانه — حتی اگر به وابستگی اپل گیر کند.
- کیفیت خروجی نمونه (PDF) واقعاً خوب است: تایپوگرافی RTL تمیز و ترجمه‌ی محاوره‌ای طبیعی.

---

## ۶) اقدامات پیشنهادی به ترتیب اولویت

1. 🔴 حذف/rotate کلید API + خواندن فقط از env؛ حذف مسیر `amir` از bat.
2. 🔴 خارج کردن PDF اسکنلیشن از بسته‌ی توزیع.
3. 🟠 اضافه‌کردن فول‌بک فونت به `assets/fonts/Vazirmatn-Bold.ttf` در `resolve_font()` (مسیر نسبی به محل اسکریپت) + فلگ `--font`.
4. 🟠 چک صریح `easyocr is None → sys.exit(پیام)` + انتخاب خودکار CPU/GPU.
5. 🟠 اصلاح `CREDIT_HINTS` (حداقل کلمه‌ی کامل: `\bteam\b` حذف یا تطبیق عبارتی سخت‌گیرانه‌تر).
6. 🟠 مستندسازی انتخاب بین دو نسخه در README (یا یکی کردن آن‌ها) + `requirements.txt` برای نسخه‌ی Pro (`easyocr pymupdf opencv-python numpy requests pillow arabic-reshaper python-bidi`).
7. 🟡 بازنویسی ساخت PDF به‌صورت استریمینگ؛ مقیاس `max_tokens` با تعداد آیتم؛ retry با backoff نمایی (الان sleep ثابت ۲ ثانیه است).
8. 🟡 اضافه‌کردن `__pycache__/` و خروجی‌ها به `.gitignore`؛ انتشار محتوای ریپو به‌جای rar داخل گیت (فعلاً گیت فقط README ۷ بایتی + rar دارد).
9. 🟡 تست واحد برای توابع خالص (`iou`، `natural_key`، `to_pixel_box`، `is_garbage`، `looks_like_credit`).

---

## ۷) جمع‌بندی

ایده و اجرای کلی پروژه **بالای میانگین و کاربردی** است — مخصوصاً پاک‌سازی ساختارمحور و طراحی «قابل resume با JSON». اما بسته در وضعیت فعلی سه مشکل جدی دارد: **کلید API افشاشده**، **خروجی دارای ریسک حقوقی همراه بسته**، و **دوگانگی مستندنشده‌ی دو پیاده‌سازی** که کاربر را سردرگم می‌کند. نسخه‌ی Pro بدون relay محلی و فونت ویندوزی عملاً از روی بسته اجرا نمی‌شود. با رفع موارد بخش ۶ (به‌ویژه ۱ تا ۴)، بسته به وضعیت قابل انتشار می‌رسد.
